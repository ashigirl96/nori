interface:
  display_name: "nori Browser"
  short_description: "Automate nori webview surfaces with snapshot/ref workflows."
  default_prompt: "Use this skill for browser automation via nori CLI: identify target surface, snapshot interactive refs, perform actions, wait for state changes, and re-snapshot to avoid stale refs."
